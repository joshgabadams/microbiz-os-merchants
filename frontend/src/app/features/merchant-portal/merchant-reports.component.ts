import { CommonModule } from '@angular/common';
import { Component, OnInit, signal } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { forkJoin } from 'rxjs';
import { MerchantPortalApiService } from '../../core/merchant-portal-api.service';
import { MerchantAnalytics, MerchantStatement } from '../../core/models/merchant-portal.models';

@Component({
  selector: 'app-merchant-reports',
  standalone: true,
  imports: [CommonModule, FormsModule],
  template: `
    <section class="page">
      <header class="heading"><div><p>Performance</p><h1>Reports & analytics</h1><span>Understand sales trends and generate account statements.</span></div><form (ngSubmit)="load()"><label>From<input type="date" name="from" [(ngModel)]="from" required /></label><label>To<input type="date" name="to" [(ngModel)]="to" required /></label><button type="submit">Apply</button></form></header>
      @if (loading()) { <div class="state">Preparing your business report...</div> }
      @else if (error()) { <div class="state error"><strong>Report unavailable</strong><p>{{ error() }}</p><button (click)="load()">Try again</button></div> }
      @else if (analytics(); as report) {
        <div class="metric-grid"><article><span>Gross sales</span><strong>{{ report.totalValue | currency:report.currency:'symbol-narrow':'1.0-0' }}</strong><small class="positive">+{{ report.changePercent }}% vs previous period</small></article><article><span>Transactions</span><strong>{{ report.totalCount }}</strong><small>{{ report.successRate }}% success rate</small></article><article><span>Average transaction</span><strong>{{ report.averageValue | currency:report.currency:'symbol-narrow':'1.0-0' }}</strong><small>Across all payment channels</small></article></div>
        <div class="analytics-grid"><section class="panel chart"><div class="panel-title"><div><h2>Sales trend</h2><p>Successful collection value by day</p></div></div><div class="bars">@for (day of report.daily; track day.label) { <div class="bar-column"><div class="bar-value">{{ day.value | currency:report.currency:'symbol-narrow':'1.0-0' }}</div><div class="bar-track"><div class="bar" [style.height.%]="barHeight(day.value)"></div></div><strong>{{ day.label }}</strong><small>{{ day.count }} sales</small></div> }</div></section><section class="panel channels"><div class="panel-title"><div><h2>Payment channels</h2><p>Share of collected value</p></div></div>@for (channel of report.channels; track channel.name) { <div class="channel"><div><strong>{{ channel.name }}</strong><span>{{ channel.value | currency:report.currency:'symbol-narrow':'1.0-0' }}</span></div><div class="progress"><i [style.width.%]="channel.percent"></i></div><small>{{ channel.percent }}%</small></div> }</section></div>
        <section class="panel locations"><div class="panel-title"><div><h2>Location performance</h2><p>Compare outlets during the selected period</p></div></div><div class="responsive"><table><thead><tr><th>Location</th><th>Transactions</th><th class="right">Sales value</th></tr></thead><tbody>@for (location of report.locations; track location.name) { <tr><td><strong>{{ location.name }}</strong></td><td>{{ location.count }}</td><td class="right amount">{{ location.value | currency:report.currency:'symbol-narrow':'1.2-2' }}</td></tr> }</tbody></table></div></section>
      }
      @if (statement(); as data) { <section class="panel statement"><div class="panel-title"><div><h2>Merchant statement</h2><p>{{ data.statementNo }} · {{ data.from | date:'dd MMM yyyy' }} to {{ data.to | date:'dd MMM yyyy' }}</p></div><div class="downloads"><button (click)="download('csv')" [disabled]="downloading()">CSV</button><button (click)="download('pdf')" [disabled]="downloading()">PDF</button></div></div><div class="statement-summary"><div><span>Opening</span><strong>{{ data.openingBalance | currency:data.currency:'symbol-narrow':'1.2-2' }}</strong></div><div><span>Credits</span><strong class="credit">+{{ data.totalCredits | currency:data.currency:'symbol-narrow':'1.2-2' }}</strong></div><div><span>Debits</span><strong class="debit">-{{ data.totalDebits | currency:data.currency:'symbol-narrow':'1.2-2' }}</strong></div><div><span>Closing</span><strong>{{ data.closingBalance | currency:data.currency:'symbol-narrow':'1.2-2' }}</strong></div></div>@if (data.entries.length === 0) { <div class="empty">No statement entries in this period.</div> } @else { <div class="responsive"><table><thead><tr><th>Date</th><th>Reference</th><th>Type</th><th>Status</th><th class="right">Amount</th></tr></thead><tbody>@for (entry of data.entries; track entry.id) { <tr><td>{{ entry.transactionDate | date:'dd MMM yyyy' }}</td><td><strong>{{ entry.transactionNo }}</strong><small>{{ entry.reference }}</small></td><td>{{ entry.type.replace('_', ' ') }}</td><td><span class="badge" [class.failed]="entry.status !== 'SUCCESSFUL'">{{ entry.status }}</span></td><td class="right amount">{{ entry.amount | currency:entry.currency:'symbol-narrow':'1.2-2' }}</td></tr> }</tbody></table></div> }</section> }
    </section>
  `,
  styles: [`
    .page{display:grid;gap:1.2rem;max-width:1200px}.heading{display:flex;justify-content:space-between;align-items:end;gap:1rem}.heading p{margin:0;color:#a06e00;font-size:.7rem;font-weight:800;text-transform:uppercase;letter-spacing:.11em}.heading h1{margin:.25rem 0;color:#182250;font-size:2rem;letter-spacing:-.04em}.heading span{color:#7d8497;font-size:.8rem}.heading form{display:flex;align-items:end;gap:.55rem}.heading label{display:grid;gap:.25rem;color:#777e91;font-size:.62rem;font-weight:700}.heading input{border:1px solid #d9dde7;border-radius:.48rem;padding:.58rem}.heading button,.downloads button,.state button{border:0;border-radius:.48rem;background:#1e2761;color:white;padding:.65rem .85rem;font-weight:750}.metric-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:1rem}.metric-grid article,.panel{background:white;border:1px solid #e0e4ed;border-radius:.8rem}.metric-grid article{display:grid;gap:.35rem;padding:1.1rem}.metric-grid span{color:#7d8497;font-size:.7rem}.metric-grid strong{color:#27314d;font-size:1.5rem}.metric-grid small{color:#8c92a3;font-size:.64rem}.metric-grid .positive{color:#24794f}.analytics-grid{display:grid;grid-template-columns:1.5fr .7fr;gap:1rem}.panel-title{display:flex;justify-content:space-between;align-items:center;padding:1rem 1.15rem;border-bottom:1px solid #eceef3}.panel-title h2{margin:0 0 .2rem;color:#303950;font-size:1rem}.panel-title p{margin:0;color:#8b91a3;font-size:.68rem}.bars{height:260px;display:flex;gap:.65rem;align-items:end;padding:1rem}.bar-column{height:100%;flex:1;display:grid;grid-template-rows:18px 1fr auto auto;gap:.25rem;text-align:center}.bar-value{font-size:.55rem;color:#8b91a3}.bar-track{height:100%;display:flex;align-items:end;justify-content:center;background:#f5f6f9;border-radius:.4rem}.bar{width:65%;min-height:4px;border-radius:.4rem .4rem .15rem .15rem;background:linear-gradient(#4d65bb,#263a83)}.bar-column strong{font-size:.65rem;color:#50586e}.bar-column small{font-size:.55rem;color:#999eae}.channels{padding-bottom:1rem}.channel{display:grid;grid-template-columns:1fr auto;gap:.4rem;padding:1rem 1.1rem 0}.channel>div:first-child{display:flex;justify-content:space-between;grid-column:1/-1}.channel strong,.channel span{font-size:.7rem;color:#4c556c}.progress{height:.4rem;background:#edf0f5;border-radius:1rem;overflow:hidden}.progress i{display:block;height:100%;background:#344b9c}.channel small{font-size:.62rem;color:#767e92}.responsive{overflow-x:auto}table{width:100%;border-collapse:collapse;min-width:620px}th,td{padding:.8rem 1rem;border-bottom:1px solid #eceef3;text-align:left;font-size:.7rem;color:#697084}th{background:#fafbfc;font-size:.6rem;text-transform:uppercase;color:#8b91a3}td strong{display:block;color:#343d54}td small{display:block;color:#969bad;margin-top:.15rem}.right{text-align:right}.amount{font-weight:750;color:#313a52}.downloads{display:flex;gap:.45rem}.downloads button{padding:.5rem .7rem}.statement-summary{display:grid;grid-template-columns:repeat(4,1fr);border-bottom:1px solid #eceef3}.statement-summary div{display:grid;gap:.3rem;padding:1rem;border-right:1px solid #eceef3}.statement-summary span{font-size:.62rem;color:#8c92a3}.statement-summary strong{font-size:.84rem;color:#333c53}.credit{color:#23754e!important}.debit{color:#a74435!important}.badge{display:inline-block;padding:.2rem .4rem;border-radius:1rem;background:#e3f5eb;color:#23754e;font-size:.55rem;font-weight:800}.badge.failed{background:#fff2ce;color:#936400}.state,.empty{min-height:180px;display:grid;place-content:center;justify-items:center;text-align:center;background:white;border:1px solid #e0e4ed;border-radius:.8rem;color:#80879a}.state.error{color:#a74435}
    @media(max-width:850px){.heading{align-items:start;flex-direction:column}.analytics-grid{grid-template-columns:1fr}.metric-grid{grid-template-columns:1fr 1fr}.metric-grid article:last-child{grid-column:1/-1}}@media(max-width:560px){.heading form{width:100%;flex-wrap:wrap}.heading label{flex:1}.metric-grid{grid-template-columns:1fr}.metric-grid article:last-child{grid-column:auto}.statement-summary{grid-template-columns:1fr 1fr}.bars{gap:.25rem;padding:.7rem}.bar-value{display:none}}
  `],
})
export class MerchantReportsComponent implements OnInit {
  analytics = signal<MerchantAnalytics | null>(null); statement = signal<MerchantStatement | null>(null); loading = signal(true); downloading = signal(false); error = signal<string | null>(null);
  from = '2026-08-13'; to = '2026-08-19';
  constructor(private readonly api: MerchantPortalApiService) {}
  ngOnInit(): void { this.load(); }
  get maxDaily(): number { return Math.max(1, ...(this.analytics()?.daily.map((item) => item.value) ?? [1])); }
  barHeight(value: number): number { return Math.max(3, value / this.maxDaily * 100); }
  load(): void { if (this.from > this.to) { this.error.set('The start date must be before the end date.'); return; } this.loading.set(true); this.error.set(null); forkJoin({ analytics: this.api.getMerchantAnalytics(this.from, this.to), statement: this.api.getMerchantStatement(this.from, this.to) }).subscribe({ next: ({ analytics, statement }) => { this.analytics.set(analytics); this.statement.set(statement); this.loading.set(false); }, error: (error) => { this.error.set(error?.error?.message ?? error?.message ?? 'The report could not be loaded.'); this.loading.set(false); } }); }
  download(format: 'csv' | 'pdf'): void { this.downloading.set(true); this.api.downloadMerchantStatement(this.from, this.to, format).subscribe({ next: (blob) => { const url = URL.createObjectURL(blob); const link = document.createElement('a'); link.href = url; link.download = `merchant-statement-${this.from}-${this.to}.${format}`; link.click(); URL.revokeObjectURL(url); this.downloading.set(false); }, error: () => { this.error.set('The statement download could not be prepared.'); this.downloading.set(false); } }); }
}
